# Notebooks

Place exploratory research, hypothesis notebooks, and diagnostic reports here. Use
lightweight datasets or mock sources when possible; production ingestion should
run via the `intentflow_ai.data` module.
